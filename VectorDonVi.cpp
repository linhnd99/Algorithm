#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("F.INP","r",stdin);
    cin >> n >> m;
    for (int i=1;i<=n;i++)
        for (int j=1;j<=m;j++)
            cin>> a[i][j];
    for (int i=1;i<m;i++)
        cin >> param[i];

}
